module Entries

  class Stats

    # Entries::Stats.standard_deviation(entries, "results[0]['score']")

    def standard_deviation(entries, metric_select_sql)

      # 1. ignore if no score recorded at al
      # 2. separate into two analyses: a) if completed, and b) if DNF

    end

  end

end